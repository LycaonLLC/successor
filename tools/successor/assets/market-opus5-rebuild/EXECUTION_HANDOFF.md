# Execution-only handoff

Two Claude Code runs have already spent the experiment's design time. Do not
repeat that reasoning and do not redesign the texture generator again.

The next run must execute, starting with the main builder:

1. Read `HUMAN_REVIEW_PASS1.md`, `ALTERNATIVES.md`, `src/plan.py`, and
   `src/mlib.py`.
2. Your first mutating tool action must replace or comprehensively rewrite
   `src/build_market.py` for chosen scheme E. The pass-1 builder describes the
   rejected building and is not protected.
3. Run the coordinate assertions and one complete Blender build immediately.
4. Render a moderate-cost pass 2 (roughly 700–800 px and low samples), inspect
   it, write `CRITIQUE_PASS2.md`, and make a substantive correction.
5. Rebuild and finish the collision, manifest, LOD, strict validation,
   post-glTF ORM wiring, Three.js load/door-animation proof, final proof set,
   editable Blend/checkpoint, and `REPORT.md`.

Use the already selected and documented E synthesis: one deep vaulted entry
hood as the focal curve; asymmetric butterfly/raked roof planes; unequal
wind/service towers and water collection; a deliberately partial shaded
loggia; stepped south/public facade; restrained structural fins only where
they have a shading/load role. Resolve it well beyond the grey blockout.

The prior run had already reasoned through a viable starting plan:

- authored shell kept inside `x=±5.70`, `y=±4.275`;
- main south wall around Blender `y=-3.12`, hood projection no farther than
  `y=-4.20`, and a projecting trainer zone in the south-east;
- north service bulkhead around `y=1.85`, with terminals docked/recessed while
  leaving each promised zero-based cell and southern approach fully clear;
- real back-of-house staff/service aisle behind the three service points;
- raised north roof plane creating an approximately 0.55 m clerestory over the
  butterfly valley/gutter;
- high public hall contrasted with a lower service ceiling;
- complete vendor, trainer, queue, storage, lighting, and rear-service fit-out.

These are starting dimensions, not excuses to skip clearance measurement.
`src/plan.py` is authoritative for fixture coordinates.

Do not spend another response narrating what you are about to write. Do not end
with “now I will build it” or a prose implementation plan. Use cheap diagnostic
renders until the corrected candidate is ready. Completion means the verified
files and evidence exist on disk; otherwise state the exact hard error only
after exhausting an implementation attempt.
